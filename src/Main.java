public class Main{
    public static void main () {
        //PlateauJeu monJeu = PlateauJeu.PLATEAU;

    /*
        public void play() {
            while (true) {
                createPlayers();
                initialiseBoard();
                while (!isCheckMate()) {
                    printBoard();
                    String move;
                    do {
                        move = askMove();
                    }
                    while (!isValidMove(move));
                    updateBoard(move);
                    switchPlayer();
                }
            }
        }
    }
    */
    }
}